import { AppBar, Box, Typography } from "@mui/material";

export function Main () {
    return (
        <Box>
            <AppBar>
                <Typography variant="h4">Share List</Typography>
            </AppBar>
          
        </Box>
    );
}
